baseurl = "https://websummoner.riadvice.com/"
locale = "en-us"
title = "WebSummoner | A Selenium Testing Solution Running Browsers in Docker"

enableRobotsTXT = true


# RSS, categories and tags disabled for an easy start
# See configuration options for more details:
# https://gohugo.io/getting-started/configuration/#toml-configuration
disableKinds = ["RSS", "taxonomy"]

[params]
description = "A cross browser testing platform running every session in its own Docker container. Fully compatible with Selenium WebDriver. Automated and live testing, with video and logs. Runs on your own hardware or in any popular cloud platform."

# Allow raw HTML (divs for TOC/cross-link cards) inside markdown pages.
[markup]
[markup.goldmark]
[markup.goldmark.renderer]
unsafe = true
